# Railway Environment Variables Template
# Copy these to your Railway project environment variables

# Database (Railway will provide PostgreSQL DATABASE_URL automatically)
# DATABASE_URL will be automatically set by Railway PostgreSQL service

# JWT Configuration
JWT_SECRET=your_secure_jwt_secret_here_change_this
JWT_ALGORITHM=HS512
JWT_EXPIRATION_MS=86400000

# App Configuration
SECRET_KEY=your_secure_secret_key_here_change_this
EXPORT_FILES_ABSOLUTE_PATH=/app/uploads/
EXPORT_FILES_SERVER_PATH=getFile

# Server Configuration (Railway will set PORT automatically)
HOST=0.0.0.0
DEBUG=False

# Temporary Stage 10 rollout hook
# Enable only for the deploy where you want startup to run the Stage 10 learning backfill.
RUN_STAGE10_BACKFILL_ON_STARTUP=false
STAGE10_BACKFILL_DELETE_LEGACY=false

# CORS Configuration - Comma-separated list of allowed origins
CORS_ORIGINS=https://sitespace.vercel.app
